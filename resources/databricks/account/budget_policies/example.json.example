{
  "enabled": false,
  "display_name": "Monthly Budget Alert",
  "filter": {
    "workspace_id": {
      "operator": "IN",
      "values": ["${module.workspaces[\"sjwstest2\"].workspace_id}"]
    },
    "tags": [
      {
        "key": "Environment",
        "value": {
          "operator": "IN",
          "values": ["Development"]
        }
      }
    ]
  },
  "alert_configurations": [
    {
      "time_period": "MONTH",
      "trigger_type": "CUMULATIVE_SPENDING_EXCEEDED",
      "quantity_type": "LIST_PRICE_DOLLARS_USD",
      "quantity_threshold": "1000",
      "action_configurations": {
        "action_type": "EMAIL_NOTIFICATION",
        "target": "admin@example.com"
      }
    }
  ],
  "comment": "Budget alert for specific workspace(s). The workspace_id is the numeric Databricks workspace ID (not Azure resource ID). You can use dynamic reference: workspace_id.values = [\"${module.workspaces[\"workspace-name\"].workspace_id}\"]"
}
